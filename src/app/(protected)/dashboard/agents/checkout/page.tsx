import { AgentCheckoutView } from 'src/views/dashboard/agents/checkout';

export const metadata = {
   title: 'Dashboard: Agent Checkout',
};

export default function Page() {
   return <AgentCheckoutView />;
}
