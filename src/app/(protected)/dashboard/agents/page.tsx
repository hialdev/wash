import { AgentView } from 'src/views/dashboard/agents/view';

// ----------------------------------------------------------------------

export const metadata = {
   title: 'Dashboard: Agent Management',
};

export default function Page() {
   return <AgentView />;
}
