import { CatalogView } from 'src/views/dashboard/catalog/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Catalog` };

export default function Page() {
   return <CatalogView />;
}
